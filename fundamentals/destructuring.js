/**
 * Both objects and arrays support destructuring, meaning that youc can extract values from them and assign them to named variables
 */

 // array
const [t, u] = ['a', 'b']
t
u

// object
const person = {
    name: 'Manjunath'
}

const { name } = person
name